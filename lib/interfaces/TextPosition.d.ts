/**
 * Describes the structure of the text node element's position on the document.
 */
export default interface TextPosition {
    top: number;
    left: number;
    width: number;
    height: number;
}
